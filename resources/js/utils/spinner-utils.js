// Spinner markup used inside buttons to indicate loading state
export const buttonSpinner = `
  <!-- Spinner element -->
  <span
    class="inline-block animate-spin mr-2 h-5 w-5 
           border-2 border-t-2 
           border-white border-t-transparent 
           rounded-full"
    role="status" aria-hidden="true">
  </span>

  <!-- Loading text -->
  Processing...
`;
